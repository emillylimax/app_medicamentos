import 'package:flutter/material.dart';

class AlertasLembretesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alertas e Lembretes')),
      body: Center(
        child: Text('Tela para configurar alertas e lembretes'),
      ),
    );
  }
}
